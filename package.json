{
  "name": "warehouse-idea",
  "private": true,
  "scripts": {
    "dev:fe": "pnpm --filter frontend dev",
    "dev:be": "pnpm --filter backend start:dev",
    "build:shared": "pnpm --filter @warehouse/shared build",
    "build": "pnpm run build:shared && pnpm --filter frontend build && pnpm --filter backend build"
  }
}
